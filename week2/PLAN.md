# Week 2 学习计划：从 KV Cache 正确性到性能证据

更新日期：2026-09-24。Lesson 3 已在低负载 GPU 1（L40）完成三次性能对照，原始数据与报告见 LESSON_03_RESULTS.md；待你阅读解释，尚未宣告全部学习验收完成。

## 本周目标

给第一周的教学 MHA 加上 KV Cache，验证结果等价，解释生命周期，设计公平的 Prefill/Decode 对照，算清有效和预留缓存大小。仍然是单层 MHA、随机 hidden states，不是完整语言模型或服务系统。

## 进度与证据

| 课次 | 内容 | 当前状态 | 验收 |
|---|---|---|---|
| Lesson 1 | 形状、增量 Attention、因果 mask | 已学习，第三课开头复习两个易错点 | 新 scores 包含历史和新 K；可见性由绝对位置决定 |
| Lesson 2 | append/view/reset、边界保护 | 手写三个函数已通过接入测试 | 历史 K/V 不变、错误不破坏状态、重置隔离、完整重算对拍 |
| Lesson 3 | Prefill/Decode 公平计时与结果解释 | GPU 对照与报告已完成，待阅读解释 | 正确性门槛、同步计时、多长度原始数据、解释有效范围 |
| Lesson 4 | 有效/预分配显存与容量取舍 | 待开始 | 计算字节、说明预分配代价和不同长度 batch 的限制 |

Lesson 2 的测试边界：kv_cache_exercise.py 当前提供独立 append/view/reset 函数；验收时在内存中接入参考类的初始化和属性，6 项 CPU 测试通过，并额外检查了满容量下历史 K/V 和新增后缀。完整类封装尚未完成；mask/Attention 等仍是参考代码，不能算作全部由你手写或全部经过 GPU 验收。

## Lesson 3 执行顺序

1. 阅读 LESSON_03.md 第 1–3 节，写预测，不先看性能数字。
2. 按第 4–6 节画出一轮实验：相同模型与输入，Prefill 和 Decode 分开，预热后重置，统一同步和计时范围。
3. 实现计划中的 benchmark_kv_cache.py。第一版用参考 KVCache；如改用手写方法，明确适配方式，重新对拍，不混合统计不同实现。
4. 先 CPU 验证流程，再选空闲 L40；B=2、C=64、H=4、FP32、N=16，L 先 32/128，后 512/1024。
5. 至少 3 轮完整预热、10 轮测量，保留各轮数据，报告中位数和波动；不把墙钟与设备计时混为一谈。
6. 整理计划中的 LESSON_03_RESULTS.md：环境、命令、正确性、原始数据、结论、限制。
7. 需要解释现象时，再单独 profile；不把 profiler 运行时的耗时填入正式基准表。

benchmark_kv_cache.py 默认只显示计划，显式加 --run 才测量。此次 GPU 正确性 6 项通过，并完成三次相同配置的性能运行，每次每个 L 测量 10 轮。报告见 LESSON_03_RESULTS.md，原始 JSON 保存在 results/；手写函数和参考计算路径未修改。

## 文件导航

- LESSON_01.md：KV Cache、形状与 causal mask。
- LESSON_02.md：追加、视图、重置和生命周期实验。
- LESSON_03.md：本次新增的公平计时与性能分析讲义。
- kv_cache.py：可运行参考实现。
- kv_cache_exercise.py：你的手写核心方法，保留原样。
- step_by_step.py：逐步打印缓存和张量形状。
- verify_kv_cache.py：默认测试参考实现，不会自动切到练习函数。
- EXERCISES.md：已有回答与练习记录，保留原样。

## 本周完成标准

- [ ] 能解释并独立实现缓存管理，整理成可导入的类并复验。
- [ ] 输出数值对齐，边界输入不会破坏有效历史。
- [ ] 分清 Prefill、Decode、上下文增长和同步计时边界。
- [ ] 有可复现的性能数字，能说明短序列与长序列的差异，而不是只报加速倍数。
- [ ] 能计算 KV 字节数，说明 reset 不释放预留存储。

完成后进入第三周的真实模型生成链路。无需把不同长度 batch、分页缓存、多 GPU 或 CUDA kernel 作为进入第三周的前置任务。
