# 第二周练习与进度

先自己作答，再用代码验证。命令成功是证据之一，还需要解释原因。

## 第一课：今天完成

- [ ] 不看答案推导：B=2、H=4、Dh=8、已有 6 个 token，再输入 1 个 token，Q_new、K_all、scores、输出的形状是什么？
- [ ] 为什么可以复用历史 K/V？为什么当前步骤通常不需要保存旧 Q？
- [ ] 手画“已有 3 个 token，再追加 2 个”的屏蔽矩阵（True=屏蔽）。
- [ ] 在 step_by_step.py 中找到每一次数值对拍：为什么完整重算只取新增位置？
- [ ] 解释演示最后的 25 与 10：错误发生在矩阵乘法还是位置/掩码语义？
- [ ] B=1、H=2、Dh=4、FP32、length=5、capacity=8 时，KV 的有效字节数与预留字节数各是多少？

我的回答：
Qnew为(2,4,1,8)Kall为（2，4，7，8）scores为(2,4,1,7)输出为2，1，32


（写在这里，保留预测与修改后的理解。）

## 第二课：生命周期与手写实现

- [ ] 先阅读参考实现，再在自己的练习文件重写 append/view/reset；保持参考实现可用作对照。
- [ ] 缓存恰好填满时应成功；再追加一枚时应报错，已有 length 和内容不变。
- [ ] 设计一个额外用例，验证历史 K/V 不会被后续追加覆盖。
- [ ] 解释为什么 reset 只改 length 即可开始一个新序列；这不等于物理擦除了存储。
- [ ] 思考：同一个 batch 中两个请求长度不同，当前单个 length 还能表示吗？本课不实现，先指出边界。

## 第三课预习：先设计公平的 benchmark

- [ ] 两条路径使用同样权重、同样输入、同样生成步数、同样 dtype/device。
- [ ] 分别记录 Prefill 与 Decode；不能把一条路径的 Prefill 隐藏，却计入另一条路径。
- [ ] 每轮重置缓存；不能让 warmup 消耗正式实验的容量或污染输入状态。
- [ ] 与第一周一样，GPU 异步计时需要明确同步边界；先验证正确性再计时。
- [ ] 预测：短序列下缓存路径一定更快吗？为什么实际结果可能与计算量估算不同？

本周最终要能解释：省了哪些重复计算、仍然读取哪些数据、为此付出了多少 KV 存储，以及测量口径是什么。
